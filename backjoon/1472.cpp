#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

struct node
{
    string data;
    vector<*node> childNode;
};

int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    vector<string> v;
    v.push_back("efg");
    v.push_back("z");
    v.push_back("abc");
    node temp;
    temp.data = ? ;
    temp.childNode.pusb_back()
        sort(v.begin(), v.end());
    return 0;
}
