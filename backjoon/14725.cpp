#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

ll n, k;

// struct node
// {
//     string data;
//     ll level;
//     vector<node> childNode;
// };

// bool cmp(node a, node b)
// {
//     return a.data < b.data;
// }

// void set(node prev, ll level)
// {
//     node temp;
//     cin >> temp.data;
//     prev.
// }

int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int *a, b = 1;
    a = &b;
    cout << a << '\n';
    cout << *a << '\n';
    // ll n;
    // node root;
    // cin >> n;
    // while (n--)
    // {
    //     cin >> k;
    //     set(tree, k);
    // }
    // node top;
    // node temp;
    // sort(top.childNode.begin(), top.childNode.end(), cmp);
    return 0;
}
